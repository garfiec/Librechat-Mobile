# Single source of truth for the app version (calver: YYYY.MM.PATCH).
# Bumped by scripts/bump-version.sh (patch | prepatch | rc | finalize);
# year/month come from the current UTC date automatically.
# versionCode packs versionName as YEAR*10000 + MONTH*100 + PATCH: 2026.06.0 -> 20260600.
# Releases before the calver cutover were semver under the same packing (0.1.3 -> 103), so
# codes stayed monotonic. Both lines are written by bump-version.sh; do not hand-edit one
# without the other. AndroidApplicationConventionPlugin re-derives the code and FAILS the
# build if it disagrees with the line below, so they cannot silently drift.
# The code is spelled out rather than left implicit because F-Droid's update detection is
# regex-only — it cannot evaluate the packing, and without a literal in a file it cannot
# see new releases at all. Note fdroidserver's default scan never reads this file: it only
# looks at the manifest and build.gradle(.kts) under the app module. The recipe has to point
# at it with UpdateCheckData — see docs/RELEASING.md.
# This is the app's own version, independent of SUPPORTED_BACKEND_VERSION.
versionName=2026.08.4
versionCode=20260804

# LibreChat backend release this build targets — best-tested experience.
# Read by the app (core/common codegen → BackendVersion.SUPPORTED_BACKEND_VERSION)
# AND by release.yml for the "Target backend" line in release notes.
# Three legal forms (BackendVersion.parse handles all three):
#   0.8.7               — synced to a stable release tag
#   0.8.8-rc1           — synced to a release-candidate tag
#   0.8.7+dev.9f8e7d6c  — partial sync to an untagged upstream commit: base version is what
#                         that commit's package.json reports; suffix pins the synced commit
#                         (must match UPSTREAM_VERSION's commit=). The `+…` metadata is
#                         stripped in comparisons, so compat checks key on the base line.
backendTargetVersion=0.8.8-rc3
